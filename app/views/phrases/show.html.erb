<header><h1><%= phrase.title %></h1><% if author_signed_in? %><nav><%= link_to "Edytuj", edit_phrase_path(phrase) %> <%= link_to "Usuń", phrase, :method => :delete, :confirm => "Na pewno tego chcesz?" %></nav><% end %></header>
<section>
	<%= sanitize phrase.rendered_description %>
</section>