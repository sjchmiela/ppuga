{
    "title": "<%= phrase.title %>",
    "group": <%= raw (phrase.group) ?   phrase.group.slug.to_json : "".to_json %>,
    "group_link": <%= raw (phrase.group) ?  phrase.group.link.to_json : "".to_json %>,
    "description": <%= raw (rendered_description(phrase).to_json) %>
}