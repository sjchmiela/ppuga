<%= form_for phrase, :html => {:class =>"form_phrase"} do |f| %>
	<% if phrase.errors.any? %>
		<div class="alert">
			<h2><%= pluralize(phrase.errors.count, "error") %> prohibited this phrase from being saved:</h2>
			<ul>
			<% phrase.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
			</ul>
		</div>
	<% end %>
	<%= f.text_field :title, :placeholder => "Tytuł", :size => nil %>
	<%= f.text_area :description, :placeholder => "Wyjaśnienie", :size => "2x2" %>
	<%= f.submit "Zapisz" %>
<% end %>